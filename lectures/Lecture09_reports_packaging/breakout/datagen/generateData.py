import numpy as np

'''This is a fairly useless program which uses np.random to generate some
data.

Example Usage:
num_to_generate = 500
data = np.random.random(num_to_generate)
'''


def randData(num_to_generate):
    mydata = np.random.random(num_to_generate)
    return mydata
